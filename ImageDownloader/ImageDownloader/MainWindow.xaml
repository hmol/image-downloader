<Controls:MetroWindow x:Class="ImageDownloader.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="Image downloader"
                      Height="400"
                      Width="600"
                      BorderThickness="0" 
                      GlowBrush="Black"
                      ResizeMode="CanResizeWithGrip"
                      WindowTransitionsEnabled="False"
                      WindowStartupLocation="CenterScreen">
    
    <StackPanel Orientation="Vertical">
        <TextBox Controls:TextBoxHelper.Watermark="Select destination folder" Margin="5" HorizontalAlignment="Left" Width="300" PreviewMouseDown="DestinationTextBox_MouseDown" />
        <TextBox x:Name="TxtSourceUrl" Controls:TextBoxHelper.Watermark="Enter url" Margin="5" HorizontalAlignment="Left" Width="300" Controls:TextBoxHelper.ClearTextButton="True" />
        <Button Style="{StaticResource AccentedSquareButtonStyle}" Width="80" HorizontalAlignment="Left" Margin="5" Content="Download" Click="Download_BtnClick" />

        <DataGrid ItemsSource="{Binding Source=MyList}" AutoGenerateColumns="False" Name="InfoDataGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Url" Binding="{Binding Url}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</Controls:MetroWindow>
